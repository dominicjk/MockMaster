---
import Math from "./Math.astro";

// Local CSS from node_modules (bundled by Vite)
import "reveal.js/dist/reveal.css";
import "reveal.js/dist/theme/white.css";
---
<div class="reveal my-8 rounded-lg overflow-hidden shadow" style="height:520px">
  <div class="slides">
    <!-- Slide 1 -->
    <section>
      <h2>Worked Example — Complex Numbers</h2>
      <p>We’ll compute modulus, argument, product, and a power.</p>
    </section>

    <!-- Slide 2 -->
    <section>
      <h3>Problem</h3>
      <p>Let <Math tex="z=3-4i" /> and <Math tex="w=1-i" />.</p>
      <ol>
        <li>Find <Math tex="|z|" /> and <Math tex="\\arg(z)" />.</li>
        <li>Write <Math tex="z" /> in polar form.</li>
        <li>Compute <Math tex="zw" />.</li>
        <li>Compute <Math tex="z^5" /> using De&nbsp;Moivre.</li>
      </ol>
    </section>

    <!-- Slide 3 -->
    <section>
      <h3>1) Modulus & Argument</h3>
      <p><Math tex="|z|=\\sqrt{3^2+(-4)^2}=5" display /></p>
      <p>
        <Math tex="z=(3,-4)" /> is in Quadrant&nbsp;IV, so
        <Math tex="\\arg(z)=-\\arctan\\!\\left(\\tfrac{4}{3}\\right)" />.
      </p>
    </section>

    <!-- Slide 4 -->
    <section>
      <h3>2) Polar Form</h3>
      <p>
        <Math tex="z=|z|(\\cos\\theta+i\\sin\\theta)=5(\\cos\\theta+i\\sin\\theta)" display />
        with <Math tex="\\theta=-\\arctan\\!\\left(\\tfrac{4}{3}\\right)" />.
      </p>
    </section>

    <!-- Slide 5 -->
    <section>
      <h3>3) Product <Math tex="zw" /></h3>
      <p><Math tex="(3-4i)(1-i)=3-3i-4i+4i^2=-1-7i" display /></p>
    </section>

    <!-- Slide 6 -->
    <section>
      <h3>4) De Moivre: <Math tex="z^5" /></h3>
      <p>
        <Math tex="z^5=|z|^5(\\cos(5\\theta)+i\\sin(5\\theta))=5^5(\\cos(5\\theta)+i\\sin(5\\theta))" display />
        with <Math tex="\\theta=-\\arctan\\!\\left(\\tfrac{4}{3}\\right)" />.
      </p>
    </section>
  </div>
</div>

<script type="module">
    import Reveal from "reveal.js/dist/reveal.esm.js";
    const root = document.currentScript.previousElementSibling; // .reveal
    const deck = new Reveal(root);
    await deck.initialize({
      hash: true,
      controls: true,
      progress: true,
      slideNumber: true,
      center: true
    });
    deck.layout(); // ensure sizing after init
  </script>
