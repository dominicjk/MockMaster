---
import Layout from '~/layouts/PageLayout.astro';
const metadata = {
  title: 'Complex Numbers - Polar Form',
};
---
<Layout metadata={metadata}>
  <section class="py-12">
    <div class="max-w-4xl mx-auto px-4 md:px-6">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4">Polar Form</h1>
        <p class="text-lg text-gray-700 dark:text-gray-300 mb-8">
          Expressing complex numbers in polar form using modulus and argument.
        </p>
        <a href="/maths/learn" class="inline-block text-blue-600 dark:text-blue-400 underline hover:text-blue-800 dark:hover:text-blue-200 transition">&larr; Back to Learn</a>
      </div>
      
      <div class="bg-white dark:bg-slate-800 rounded-xl shadow-lg p-8">
        <h2 class="text-2xl font-bold mb-6 text-red-600 dark:text-red-400">Polar Representation</h2>
        <p class="text-gray-700 dark:text-gray-300 mb-6">
          Complex numbers can be expressed as r(cos θ + i sin θ) where r is the modulus and θ is the argument.
        </p>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <div class="bg-gray-50 dark:bg-slate-700 rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-3 text-red-600 dark:text-red-400">Modulus</h3>
            <p class="text-gray-700 dark:text-gray-300 mb-4">
              The distance from the origin to the point.
            </p>
            <div class="text-gray-700 dark:text-gray-300 space-y-2">
              <p>For z = a + bi:</p>
              <p>|z| = √(a² + b²)</p>
              <p><strong>Example:</strong></p>
              <p>For 3 + 4i: |z| = √(9 + 16) = 5</p>
            </div>
          </div>
          
          <div class="bg-gray-50 dark:bg-slate-700 rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-3 text-red-600 dark:text-red-400">Argument</h3>
            <p class="text-gray-700 dark:text-gray-300 mb-4">
              The angle from the positive real axis.
            </p>
            <div class="text-gray-700 dark:text-gray-300 space-y-2">
              <p>arg(z) = tan⁻¹(b/a)</p>
              <p><strong>Example:</strong></p>
              <p>For 3 + 4i: arg(z) = tan⁻¹(4/3)</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout> 